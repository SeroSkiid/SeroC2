<Window x:Class="SeroServer.UI.TagDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Set Tag"
        Width="340" Height="160"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Border Background="{StaticResource BgBrush}" CornerRadius="10"
            BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Padding="20">
        <StackPanel>
            <TextBlock Text="TAG" Style="{StaticResource SLabel}" />
            <TextBox x:Name="TxtTag" Style="{StaticResource STxt}" Margin="0,0,0,14"
                     KeyDown="TxtTag_KeyDown" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="CANCEL" Width="80" Style="{StaticResource SBtn}" Click="Cancel_Click" />
                <Button Content="OK" Width="80" Margin="8,0,0,0" Style="{StaticResource SGreenBtn}" Click="Ok_Click" />
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
